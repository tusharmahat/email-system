<%# email.ejs for listing emails @author Tushar %> <%var i=0 %><% if(typeof
inbox != 'undefined') {%>    
<div class="form-group search-box">
  <i class="icon fas fa-search"></i>
  <input type="input" class="form-control input-lg text-search"  placeholder="Type email to search">
</div>
<form
class="btn-delete-all"
action="/users/inbox/delete-all"
method="POST"
onsubmit="return confirmDelete('all','emails')"
>
<button type="submit">
  <i class="icon fas fa-trash-alt"></i>
</button>
</form>
<%inbox.forEach( function(email){%>
<div class="view-<%=i%>">
  <form class="btn-fav" action="/users/inbox/add-to-fav/<%=i%>" method="POST">
    <button type="submit">
      <i
        class="icon <%= email.fav? 'icon-fav': '' %>  far fa-star"
      ></i>
    </button>
  </form>
  <a href="/inbox/-<%=i%>"
    ><label class="list-mail"  <%if(typeof index!='undefined'){if(i-index==0){%>style=" background: lightgray; border: 1px solid lightgray; border-radius: 5px;"<%}%><%}%> >
      <label  class="<%=inbox[i].read==true ? '': 'unread'%>"
      >
      <%=email.from%> </label
      >
      <label  class="list-sb"><%=email.sb%></label></label
    ></a
  >
  <form
    class="btn-delete"
    action="/users/inbox/delete-one/<%=i%>"
    method="POST"
    onsubmit="return confirmDelete('this','email')"
  >
    <button type="submit">
      <i class="icon far fa-times-circle"></i>
    </button>
  </form>
</div>
<%i++%> <% })%> <%}%> <% if(typeof sent != 'undefined') {%>
  <div class="form-group search-box">
    <i class="icon fas fa-search"></i>
    <input type="input" class="form-control input-lg text-search"  placeholder="Type email to search">
  </div>
<form
  class="btn-delete-all"
  action="/users/sent/delete-all"
  method="POST"
  onsubmit="return confirmDelete('all','emails')"
>
  <button type="submit">
    <i class="icon fas fa-trash-alt"></i>
  </button>
</form>
<%sent.forEach( function(email){%>
<div class="view-<%=i%>">
  <a href="/sent/-<%=i%>">
    <label class="list-mail" <%if(typeof index!='undefined'){if(i-index==0){%>style="background: lightgray; border: 1px solid lightgray; border-radius: 5px;"<%}%><%}%> ><label href="/sent/<%=i%>" data-role="button" class=""
        ><%=email.to%></label
      >
      <label class="list-sb"><%=email.sb%></label></label
    ></a
  >
  <form
    class="btn-delete"
    action="/users/sent/delete-one/<%=i%>"
    method="POST"
    onsubmit="return confirmDelete('this','email')"
  >
    <button type="submit">
      <i class="icon far fa-times-circle"></i>
    </button>
  </form>
</div>
<%i++%> <%})%> <%}%> <% if(typeof fav != 'undefined') {%> <%fav.forEach(
function(email){ %> <%if(email.fav){%>
<div class="view-<%=i%>">
  <form class="btn-fav" action="/users/fav/add-to-fav/<%=i%>" method="POST">
    <button type="submit">
      <i
        class="icon <%= email.fav? 'icon-fav': '' %>  far fa-star"
      ></i>
    </button>
  </form>
  <a href="/fav/-<%=i%>"
    ><label class="list-mail"  <%if(typeof index!='undefined'){if(i-index==0){%>style=" background: lightgray; border: 1px solid lightgray; border-radius: 5px;"<%}%><%}%>>
      <label class="<%=fav[i].read==true ? '': 'unread'%>"
        ><%=email.to%></label
      >
      <label data-role="button" class="list-sb"><%=email.sb%></label></label
    ></a
  >
  <form
    class="btn-delete"
    action="/users/inbox/delete-one/<%=i%>"
    method="POST"
    onsubmit="return confirmDelete('this','email')"
  >
    <button type="submit">
      <i class="icon far fa-times-circle"></i>
    </button>
  </form>
</div>
<%}%><%i++%><%})%> <%}%> <% if(typeof unread != 'undefined') {%>
<%unread.forEach( function(email){%><%if(!email.read){%>
<div class="view-<%=i%>">
  <form class="btn-fav" action="/users/inbox/add-to-fav/<%=i%>" method="POST">
    <button type="submit">
      <i
      class="icon <%= email.fav? 'icon-fav': '' %>  far fa-star"
      onclick="addToFav('inbox',<%=i%>)"
    ></i
    >
    </button>
  </form>
  <a href="/unread/-<%=i%>"
    ><label class="list-mail"  <%if(typeof index!='undefined'){if(i-index==0){%>style=" background: lightgray; border: 1px solid lightgray; border-radius: 5px;"<%}%><%}%>>
      <label class="<%=unread[i].read==true ? '': 'unread'%>"
        ><%=email.from%></;
      >
      <label data-role="button" class="list-sb"><%=email.sb%></label></label
    ></a
  >
  <form
    class="btn-delete"
    action="/users/inbox/delete-one/<%=i%>"
    method="POST"
    onsubmit="return confirmDelete('this','email')"
  >
    <button type="submit">
      <i class="icon far fa-times-circle"></i>
    </button>
  </form>
</div>
<%}%><%i++%> <% })%> <%}%> <% if(typeof deleted != 'undefined') {%>
  <div class="form-group search-box">
    <i class="icon fas fa-search"></i>
    <input type="input" class="form-control input-lg text-search"  placeholder="Type email to search">
  </div>
<form
  class="btn-delete-all"
  action="/users/deleted/delete-all"
  method="POST"
  onsubmit="return confirmDelete('all','emails')"
>
  <button type="submit">
    <i class="icon fas fa-trash-alt"></i>
  </button>
</form>
<%deleted.forEach( function(email){%>
<div class="view-<%=i%>">
  <a href="/deleted/-<%=i%>">
    <label class="list-mail" <%if(typeof index!='undefined'){if(i-index==0){%>style=" background: lightgray; border: 1px solid lightgray; border-radius: 5px;"<%}%><%}%> ><label href="/deleted/<%=i%>" data-role="button" class=""
        ><%=email.to%></label
      >
      <label class="list-sb"><%=email.sb%></label></label
    ></a
  >
  <form
    class="btn-delete"
    action="/users/deleted/delete-one/<%=i%>"
    method="POST"
    onsubmit="return confirmDelete('this','email')"
  >
    <button type="submit">
      <i class="icon far fa-times-circle"></i>
    </button>
  </form>
</div>
<%i++%> <%})%> <%}%>
